// ... (All other expense and helper code)

/**
 * Main handler for the Vercel Serverless Function.
 * @param {import('http').IncomingMessage} req
 * @param {import('http').ServerResponse} res
 */
export default async function handler(req, res) {
    // ... (Function logic)
}
